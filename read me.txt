1 :CREATION DU PROJET

*cd projets_laravel
*composer create-project  laravel/laravel Dgtcp_project

2:MISE EN PLACE DES CONTROLLEURS , MODELS , ET LES VUES

3:MISE EN PLACE DE LARAVEL:UI (POOUR LE SYSTEME DE CONNEXION)

*composer require laravel/ui
*php artisan ui bootstrap
*php artisan ui bootstrap --auth
*npm install
*npm run dev

4:MISE EN PLACE DU KIT DE CONVERSION DE DEVISE

*https://www.nicesnippets.com/blog/how-to-fetch-currency-exchange-rates-in-laravel
*composer require amrshawky/laravel-currency

5:MISE EN PLACE DU KIT POUR LE PDF

*composer require barryvdh/laravel-dompdf
*php artisan vendor:publish --provider="Barryvdh\DomPDF\ServiceProvider"
*https://github.com/barryvdh/laravel-dompdf
*https://www.youtube.com/watch?v=L8xeiehARG8
*https://www.akilischool.com/cours/laravel-generer-un-pdf-avec-laravel-dompdf

6: MISE EN PLACE DE L'ENVOIE DU MAIL AVEC MAILTRAP

*php artisan make:mail --help
*php artisan make:mail --markedown

-tu vas voir au niveau du dossier view un dossier mail ou on aura un fichier welcome-user-mail.blade.php c'est dans ce fichier que on mettra le mail cad le template du mail que la personne recevra
-Au niveau du dossier App tu verra un dossier mail ou tu aura un fichier WelcomeUserMail.php à l'interieur tu definiras le fichier comme ce ceci 

/*

<?php

namespace App\Mail;

use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Mail\Mailable;
use Illuminate\Mail\Mailables\Content;
use Illuminate\Mail\Mailables\Envelope;
use Illuminate\Queue\SerializesModels;


class WelcomeUserMail extends Mailable
{
    use Queueable, SerializesModels;

    public $user;
    /**
     * Create a new message instance.
     */
    public function __construct()
    {
     
    }


    /**
     * Get the message envelope.
     */
    public function build()
    {
        return $this->markdown('mail.welcome-user-mail')
                    ->subject('Bienvenue');
    }

    /**
     * Get the message content definition.
     */
    public function content(): Content
    {
        return new Content(
            markdown: 'mail.welcome-user-mail',
        );
    }


    /**
     * Get the attachments for the message.
     *
     * @return array<int, \Illuminate\Mail\Mailables\Attachment>
     */
    public function attachments(): array
    {
        return [];
    }
}


*/

-Comme le message s'envoit apres l'inscription du user au niveau de App\Controllers\Auth\RegisterController precisement au niveau de la fonction create tu formaterras la fonction create comme ceci :

/*

 protected function create(array $data)
    {
        $user = User::create([
            'name' => $data['name'],
            'email' => $data['email'],
            'password' => Hash::make($data['password']),
        ]);

        // Envoyer l'e-mail de bienvenue
        Mail::to($user->email)->send(new WelcomeUserMail());
        return $user;
    }
    
  */

7:NOUS DEVONS PERMETTRE À CE QUE CHAQUE USER QUI SE CONNECTE SE RETROUVE SUR LA BONNE PAGE 

-Alors au niveau de mon ficher App\Controllers\Auth\LoginController apres la fonction ___Contruct() tu mettras une autre fonction qui ser commme ceci

/*

 public function redirectTo()
    {
        if (Auth::check() && Auth::user()->poste == 'Sécretaire') {
      
            return '/HSecretaire';
    
        }
       
        if (Auth::check() && Auth::user()->poste == 'Client') {
        
            return '/Client';
    
        }
    }
*/

-Alors au niveau de mon fichier Web.php

J'aurais des routes comme ceci

//Route::get('/Client', [App\Http\Controllers\ControllerClient::class, 'home']);
//Route::get('/HSecretaire', [App\Http\Controllers\ControllerSecretaire::class, 'home']);

-Alors au niveau de mon ControllerClient la fonction 'home' sera comme ceci 

/*
   public function home()
    {
        $id = Auth::id();
        $user = User::where('id', '=', $id)->get();

        return view('client.Home', compact('user'));
    }

*/













